[![Watch the video](https://i3.ytimg.com/vi/JovhfHhxqdM/hqdefault.jpg)](https://www.youtube.com/watch?v=JovhfHhxqdM)

Run the latest ComfyUI. First start installs dependencies (takes a few minutes), then when you see this in the logs, ComfyUI is ready to be used: `[ComfyUI-Manager] All startup tasks have been completed.`

## Access

- `8188`: ComfyUI web UI
- `42424`: Pinokio web UI
- `11434`: Ollama API
- `3000`: Open WebUI
- `8080`: FileBrowser (admin / adminadmin12)
- `8888`: JupyterLab (token via `JUPYTER_PASSWORD`, root at `/workspace`)
- `22`: SSH (set `PUBLIC_KEY` or check logs for generated root password)

## Pre-installed custom nodes

- ComfyUI-Manager
- ComfyUI-KJNodes
- Civicomfy

## Custom Arguments

Edit `/workspace/comfy/comfyui_args.txt` (one arg per line):

```
--max-batch-size 8
--preview-method auto
```

## Directory Structure

- `/workspace/comfy/ComfyUI`: ComfyUI install
- `/workspace/comfy/comfyui_args.txt`: ComfyUI args
- `/workspace/comfy/filebrowser.db`: FileBrowser DB
- `/workspace/pinokio/`: Pinokio install and data
- `/workspace/ollama/`: Ollama models storage
- `/workspace/open-webui/`: Open WebUI data and config
